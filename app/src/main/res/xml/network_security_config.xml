<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext to localhost (embedded proxy) and decode API -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="true">node.mrkalpha.tech</domain>
    </domain-config>

    <!-- All other traffic: HTTPS only, system CAs -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Debug builds: also accept user-installed certs for testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
</network-security-config>
